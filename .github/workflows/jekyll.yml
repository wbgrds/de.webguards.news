name: Build Jekyll Site

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Test"
      - run: apt-get update
      - run: apt-get install -y ruby-full
